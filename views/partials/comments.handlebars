<div class="box">
        <div class="box-header post-info-wrapper">
        </div>
        <div class="post-body">
            <p>{{comment_body}}</p>
            <p>—{{user.username}}  {{format_date created_at}}</p>
        </div>
</div> 